{
  "name": "react-flow-relay-graphql",
  "version": "0.1.0",
  "license": "",
  "dependencies": {
    "flow-bin": "^0.104.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-relay": "^5.0.0",
    "react-scripts": "^3.0.1"
  },
  "scripts": {
    "setup": "node ./setup",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "flow": "flow",
    "relay": "relay-compiler --src ./src --schema ./src/app/schema.graphql"
  },
  "devDependencies": {
    "babel-plugin-relay": "^5.0.0",
    "relay-compiler": "^5.0.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
